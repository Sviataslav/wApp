<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="./css/styles.css">
    </head>
    <body>
        {{>header}}
        {{>error}}
        {{errMass}}
        {{>footer}}
    </body>
</html>